# PRD Analysis Report: UI Overhaul (v2)

> **분석 대상**: `docs/prd/PRD_ui-overhaul.md` v1.0
> **분석일**: 2026-02-13
> **참고 자료**: `docs/reference/` Liner 스크린샷 4장
> **상태**: PRD 수정안 확정 대기

---

## 1. Liner 레퍼런스 정밀 분석

### 1.1 사이드바 해부 (Anatomy)

Liner는 두 가지 상태의 사이드바를 가진다:

**접힌 상태 (Collapsed — 스크린샷 1):**
```
┌──────┐
│ 🐦   │  ← 로고 아이콘 (브랜드 컬러)
│      │
│ 검색∨│  ← 현재 모드 표시 (드롭다운)
│      │
│  +   │  ← 새 질문/프로젝트
│  📄  │  ← 컬렉션/프로젝트
│  ⭐  │  ← 즐겨찾기
│      │
│      │
│      │
│      │
│  ⬇   │  ← 앱 다운로드
└──────┘
  48px
```

**펼친 상태 (Expanded — 스크린샷 2, 3):**
```
┌─────────────────────────┐
│ 🐦 Liner          [⊞]  │  ← 로고 + 브랜드명 + 접기 토글
│                         │
│ + 새 질문         ⌘K   │  ← Primary Action + 단축키
│ 📄 소스 컬렉션          │  ← 저장소/히스토리
│                         │
│ ── 탐색 ──              │  ← 섹션 레이블 (gray, 소문자)
│ 🟢 라이너 스콜라    ↗   │  ← 기능 그룹 (외부 링크)
│    리서치 워크스페이스    │     ← 하위 항목 (들여쓰기)
│    문헌 조사             │     ← 활성 시 좌측 보더
│    인용 추천             │
│    가설 생성             │
│    더 보기               │     ← 접힌 항목 표시
│                         │
│ ── 하단 영역 ──          │
│ "리서치를 시작하세요"     │  ← CTA 텍스트
│ [G 구글로 계속하기]      │  ← Primary CTA (green)
│ [    로그인     ]        │  ← Secondary CTA (outline)
│                         │
│ 앱 다운로드              │  ← 최하단 링크
└─────────────────────────┘
         ~260px
```

**핵심 디자인 규칙:**
| 요소 | 스타일 |
|------|--------|
| 섹션 레이블 | 소문자, `#9CA3AF` (gray-400), 12px, 좌측 정렬 |
| 항목 텍스트 | 14px, `#374151` (gray-700), 좌측 아이콘+텍스트 |
| 활성 항목 | 좌측 2px 컬러 보더 + 배경 `#F3F4F6` |
| 하위 항목 | 20px 들여쓰기, 아이콘 없음 |
| 접기 토글 | 우상단 `⊞` 아이콘 |
| CTA 버튼 | 사이드바 하단 고정, full-width |

### 1.2 콘텐츠 영역 패턴

Liner 홈페이지 (스크린샷 1, 2):
```
┌──────────────────────────────────────────┐
│                                          │
│              (상단 여백 ~25vh)            │
│                                          │
│        모든 검색을 정확하게               │  ← 헤드라인: 볼드, ~32px, 중앙정렬
│  문장마다 팩트 체크된 답변을 받아보세요     │  ← 서브: 일반, ~16px, gray-500
│                                          │
│  ┌──────────────────────────────────┐    │
│  │ [@] [🌐 웹] [📄 논문] [⚙]      │    │  ← 인풋 탭 (상단)
│  │                                  │    │
│  │ 무엇이든 물어보세요               │    │  ← 플레이스홀더
│  │                                  │    │
│  │ 📎 기본 검색 ∨              [↑]  │    │  ← 하단: 첨부+모드+제출
│  └──────────────────────────────────┘    │
│                                          │
│              (하단 여백)                  │
│                                          │
│  ┌──────────────────────────────────┐    │
│  │ New  라이너 스콜라          [→]  │    │  ← 프로모 배너 (하단 고정)
│  │ 연구를 위한 AI로 검색하고...      │    │
│  └──────────────────────────────────┘    │
│                                          │
│          요금제 · 한국어∨ · 팀 플랜       │  ← 미니 푸터
└──────────────────────────────────────────┘
```

Liner Scholar — 에이전트 선택 시 (스크린샷 3):
```
┌──────────────────────────────────────────┐
│                                          │
│              (상단 여백 ~20vh)            │
│                                          │
│  주제만 입력하면 연구 흐름과              │  ← 선택된 에이전트에 맞는 헤드라인
│  핵심 논문을 한눈에                       │
│  새로운 분야의 주요 흐름과 필수 논문을     │  ← 서브텍스트
│  빠르게 확인하세요                        │
│                                          │
│  ┌──────────────────────────────────┐    │
│  │ 연구 주제를 입력하세요            │    │  ← 도구별 전용 인풋
│  │                                  │    │
│  │ [예시 문장 입력]       조사 시작 →│    │  ← 예시 칩 + CTA
│  └──────────────────────────────────┘    │
│                                          │
└──────────────────────────────────────────┘
```

**핵심 패턴:**
- 콘텐츠가 **수직 중앙** 정렬 (flex center)
- 사이드바 선택에 따라 **헤드라인 + 인풋이 바뀜**
- 인풋 카드는 `border: 1px solid #E5E7EB`, `border-radius: 12px`, 넉넉한 내부 패딩
- 배경은 순백 `#FFFFFF`, 그림자 없음

### 1.3 상단 바 (Top Bar)

Liner는 사이드바가 있지만, **상단 우측에도 얇은 바**가 존재한다:
```
                              요금제  로그인  [회원가입]
```
- 높이: ~48px
- 좌측: 비어있음 (사이드바가 로고 담당)
- 우측: 요금제 링크, 로그인, 회원가입 (green filled button)
- WIGVU에는 인증 없으므로 이 영역은 최소화 또는 제거 가능

---

## 2. PRD 섹션별 수정안

### 2.1 Section 3 — Information Architecture (전면 재설계)

**현재 PRD:**
```
/                    → 메인 (탭 네비게이션)
  ├── [Study 탭]     → 학습 글 목록 + 히어로
  └── [Video 탭]     → 동영상 URL 입력
```

**수정안:**
```
/                    → 메인 (사이드바 "읽기" 기본 선택)
                       └─ 학습 글 목록 즉시 노출
/video               → 영상 분석 (사이드바 "영상 분석" 선택)
                       └─ URL 입력 + 기능 안내
/read/[articleId]    → 글 읽기 & 학습 모드
/analyze/[videoId]   → 동영상 분석 결과
```

**네비게이션 변경:**

| 현재 PRD | 수정안 | 이유 |
|---------|--------|------|
| 상단 헤더 (로고+유틸리티) | 사이드바 상단에 로고 통합 | Liner 패턴 |
| 상단 탭 바 (Study/Video) | 사이드바 항목으로 전환 | Liner 패턴 |
| 탭 전환 (클라이언트, URL 변경 없음) | URL 기반 라우팅 (`/`, `/video`) | 사이드바=페이지 전환이 자연스러움 |
| 상단 테마 토글 | 사이드바 하단 유틸리티 | Liner 하단 영역 |

### 2.2 Section 3.2 — Navigation (완전 교체)

**삭제할 내용:**
```
┌─────────────────────────────────────────────────┐
│  WIGVU                          [Theme] [GitHub] │
├─────────────────────────────────────────────────┤
│          [ Study ]    [ Video ]                  │
└─────────────────────────────────────────────────┘
```

**새 내용 — WIGVU 사이드바 구조:**

```
┌──────────────────────────┐
│ 🇰🇷 WIGVU           [⊞] │  ← 로고 + 접기 토글
│                          │
│ ── 학습 ──               │  ← 섹션: 학습
│ 📖 읽기                  │  ← 기본 선택 (/) — 학습 글 목록
│                          │
│ ── 도구 ──               │  ← 섹션: 도구
│ 🎬 영상 분석             │  ← (/video) — YouTube URL 입력
│                          │
│                          │
│                          │  ← 여유 공간 (향후 확장)
│                          │
│ ── ── ── ──              │  ← 구분선
│ 🌐 EN ∨                 │  ← 학습 언어 선택 (드롭다운)
│ ☀️ 라이트 모드            │  ← 테마 토글
│ GitHub ↗                │  ← 외부 링크
└──────────────────────────┘
```

**접힌 상태:**
```
┌──────┐
│ 🇰🇷   │  ← 로고 아이콘만
│      │
│ 📖   │  ← 읽기
│      │
│ 🎬   │  ← 영상 분석
│      │
│      │
│ 🌐   │  ← 언어
│ ☀️   │  ← 테마
│      │  ← GitHub (접힌 상태에서 숨김)
└──────┘
 48px
```

**사이드바 항목 상세:**

| 항목 | 아이콘 | 라우트 | 활성 조건 | 동작 |
|------|--------|--------|----------|------|
| 읽기 | `BookOpen` | `/` | pathname === `/` 또는 `/read/*` | 학습 글 목록 표시 |
| 영상 분석 | `Film` | `/video` | pathname === `/video` 또는 `/analyze/*` | YouTube URL 입력 |
| 언어 선택 | `Globe` | - | - | 드롭다운으로 7개 언어 선택 |
| 테마 토글 | `Sun`/`Moon` | - | - | 라이트/다크 전환 |
| GitHub | `Github` | external | - | 새 탭에서 열림 |

**활성 상태 스타일 (Liner 참고):**
```css
/* 비활성 */
.sidebar-item {
  color: var(--foreground-secondary);      /* #6B7280 */
  background: transparent;
  border-left: 2px solid transparent;
}

/* 활성 */
.sidebar-item.active {
  color: var(--foreground);                /* #111111 */
  background: var(--background-secondary); /* #F8F9FA */
  border-left: 2px solid var(--accent);    /* #2563EB */
  font-weight: 500;
}

/* 호버 */
.sidebar-item:hover {
  background: var(--background-secondary); /* #F8F9FA */
}
```

### 2.3 Section 4.1 — 메인 페이지 `/` (Study → 읽기)

**현재 PRD 문제:**
- 히어로(헤드라인+서브텍스트+언어칩) 가 화면 상단 30%+ 차지
- 글 목록까지 스크롤 필요 가능성

**수정안 — Liner 패턴 적용:**

사이드바에서 "읽기"가 이미 선택된 상태이므로, 콘텐츠 영역은 **바로 글 목록**을 보여준다.

```
┌─────────┬──────────────────────────────────────────┐
│         │                                          │
│ Sidebar │     한국어, 읽으면서 배우세요              │  ← 1줄 헤드라인 (24px, 볼드)
│         │     AI가 문장별로 번역하고                 │  ← 1줄 서브 (14px, gray)
│ 📖 읽기  │     핵심 표현을 알려드려요                 │
│  (활성)  │                                          │
│         │  ┌────────────┐  ┌────────────┐          │
│ 🎬 영상  │  │ Beginner   │  │ Intermediate│          │
│         │  │            │  │             │          │
│         │  │ 한국의      │  │ 존댓말의    │          │
│         │  │ 사계절과 문화│  │ 이해        │          │
│         │  │            │  │             │          │
│         │  │ 한국은 봄...│  │ 한국어에는...│          │
│         │  │            │  │             │          │
│ ── ── ──│  │ → 읽기 시작 │  │ → 읽기 시작  │          │
│ 🌐 EN∨ │  └────────────┘  └────────────┘          │
│ ☀️ 라이트│                                          │
│ GitHub  │  ┌────────────┐  ┌────────────┐          │
│         │  │ Beginner   │  │ Advanced   │          │
│         │  │ 서울의      │  │ 한국의     │          │
│         │  │ 대중교통    │  │ 디지털 문화 │          │
│         │  └────────────┘  └────────────┘          │
└─────────┴──────────────────────────────────────────┘
```

**변경 포인트:**

| 요소 | 현재 PRD | 수정안 |
|------|---------|--------|
| 헤드라인 | Display 36px, 큰 히어로 | Heading1 24px, 콘텐츠 영역 상단에 간결하게 |
| 서브텍스트 | 별도 줄, 16px | 헤드라인 바로 아래, 14px, 1줄 |
| 언어 선택 | 히어로 안의 칩 그룹 (7개 가로) | 사이드바 하단 드롭다운 |
| 글 목록 | 히어로 아래 구분선 이후 | 헤드라인 바로 아래, 스크롤 없이 노출 |
| 히어로 높이 | 화면의 ~35% | 화면의 ~12% (헤드라인+서브 2줄) |

**언어 선택을 사이드바로 이동하는 이유:**
- Liner에서 검색 모드(@, 웹, 논문)가 인풋 안에 있듯, 기능 전환은 사이드바/인풋에 붙음
- 언어는 한번 설정하면 자주 바꾸지 않는 설정값 → 사이드바 하단이 적합
- 콘텐츠 영역에서 불필요한 높이를 차지하지 않음

### 2.4 Section 4.2 — Video 페이지 `/video` (신규 라우트)

**현재 PRD:** Video 탭 (같은 페이지의 탭 전환)

**수정안:** 별도 페이지 `/video`

```
┌─────────┬──────────────────────────────────────────┐
│         │                                          │
│ Sidebar │              (상단 여백 ~20vh)            │
│         │                                          │
│ 📖 읽기  │     YouTube 영상, 핵심만 빠르게           │  ← 헤드라인 (24px, 볼드, 중앙)
│         │     URL을 붙여넣으면 AI가 요약하고         │  ← 서브 (14px, gray, 중앙)
│ 🎬 영상  │     핵심 구간을 찾아드려요                 │
│  (활성)  │                                          │
│         │  ┌──────────────────────────────────┐    │
│         │  │                                  │    │
│         │  │  YouTube URL을 입력하세요...      │    │  ← 인풋 카드
│         │  │                                  │    │     (Liner 검색창과 동일 스타일)
│         │  │                        [분석 →]  │    │     border, radius-12, 패딩
│         │  └──────────────────────────────────┘    │
│ ── ── ──│                                          │
│ 🌐 EN∨ │     ┌────────┐ ┌────────┐ ┌────────┐    │
│ ☀️ 라이트│     │ 📝     │ │ 🎯     │ │ 🌐     │    │  ← 기능 카드 3개
│ GitHub  │     │ AI 요약 │ │ 핵심구간│ │ 자막번역│    │
│         │     └────────┘ └────────┘ └────────┘    │
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**Liner 홈페이지와의 매핑:**

| Liner 홈페이지 | WIGVU `/video` 페이지 |
|---------------|----------------------|
| "모든 검색을 정확하게" | "YouTube 영상, 핵심만 빠르게" |
| 검색 인풋 카드 (탭+플레이스홀더+제출) | URL 인풋 카드 (플레이스홀더+분석 버튼) |
| 하단 프로모 배너 | 기능 소개 카드 3개 |

**핵심:** `/video` 페이지는 Liner 홈페이지처럼 **콘텐츠 수직 중앙 정렬**. 인풋이 화면 중심에 놓이는 구조.

### 2.5 Section 4.3 — 글 읽기 `/read/[articleId]`

**변경 최소 — 사이드바 연동만 추가:**

| 요소 | 변경 |
|------|------|
| 사이드바 | "읽기" 항목 활성 유지 |
| 콘텐츠 영역 | 기존 PRD 유지 (720px 중앙, 18px, line-height 1.8) |
| 뒤로가기 | 사이드바 "읽기" 클릭 또는 브라우저 뒤로 → `/`로 이동 |

### 2.6 Section 6 — Functional Requirements (교체)

**삭제 항목:**
| ID | 삭제 이유 |
|----|----------|
| FR-001 | "상단 헤더에 로고, 테마 토글, GitHub" → 사이드바로 이동 |
| FR-002 | "Study/Video 탭 네비게이션" → 사이드바로 교체 |
| FR-003 | "Study 탭 기본 활성" → 사이드바 "읽기" 기본 |
| FR-004 | "탭 전환 시 URL 변경 없이" → URL 기반으로 변경 |
| FR-005 | "탭 활성 상태 인디케이터" → 사이드바 활성 스타일 |

**새 FR — 사이드바 & 레이아웃:**

| ID | Requirement | Priority |
|----|------------|----------|
| FR-001 | 좌측 사이드바: 로고, 네비게이션, 유틸리티 통합 | P0 |
| FR-002 | 사이드바 "읽기" 클릭 시 `/` 이동, "영상 분석" 클릭 시 `/video` 이동 | P0 |
| FR-003 | `/` 접속 시 사이드바 "읽기" 활성, 글 목록 즉시 표시 | P0 |
| FR-004 | 사이드바 활성 항목: 좌측 2px 액센트 보더 + 배경색 | P0 |
| FR-005 | 사이드바 접기/펼치기 토글 (접힌=48px 아이콘, 펼친=240px) | P1 |
| FR-006 | 데스크탑 기본 펼침, 태블릿 기본 접힘, 모바일 기본 숨김 | P1 |
| FR-007 | 모바일: 햄버거 버튼으로 사이드바 오버레이 표시 | P0 |
| FR-008 | 상단 헤더 제거, 모든 네비게이션은 사이드바에서 처리 | P0 |

**새 FR — Study (읽기) 페이지:**

| ID | Requirement | Priority |
|----|------------|----------|
| FR-010 | `/` 페이지: 간결한 헤드라인(24px) + 서브텍스트(14px) + 글 목록 | P0 |
| FR-011 | 학습 언어 선택: 사이드바 하단 드롭다운 (7개 언어) | P0 |
| FR-012 | 글 목록: 2열 그리드 (데스크탑), 1열 (모바일) | P0 |
| FR-013 | 카드: 레벨 배지 + 제목 + 미리보기(2줄) + CTA | P0 |
| FR-014 | 카드 클릭 시 `/read/[articleId]?lang={선택된 언어}` 이동 | P0 |
| FR-015 | 헤드라인+서브텍스트가 뷰포트 12% 이내, 글 목록 스크롤 없이 노출 | P1 |

**새 FR — Video (영상 분석) 페이지:**

| ID | Requirement | Priority |
|----|------------|----------|
| FR-020 | `/video` 페이지: 헤드라인 + URL 인풋 카드 (수직 중앙 정렬) | P0 |
| FR-021 | URL 인풋 카드: Liner 검색창 스타일 (border, radius-12, 넉넉한 패딩) | P0 |
| FR-022 | URL 입력 후 Enter 또는 분석 버튼 → `/analyze/[videoId]` 이동 | P0 |
| FR-023 | 인풋 카드 아래 기능 소개 카드 3개 (아이콘+제목+설명) | P1 |
| FR-024 | 기존 캐러셀, 프라이싱 섹션 삭제 | P0 |

### 2.7 Section 7.3 — Responsive (사이드바 대응)

**현재 PRD:** 탭 기반 반응형

**수정안:**

| Breakpoint | 사이드바 | 콘텐츠 영역 | 글 목록 |
|------------|---------|-----------|--------|
| Desktop >1024px | 펼침 (240px) | `calc(100vw - 240px)`, 좌측 정렬 | 2열 |
| Tablet 640-1024px | 접힘 (48px) | `calc(100vw - 48px)` | 2열 |
| Mobile <640px | 숨김 (0px) + 햄버거 | 100vw | 1열 |

**모바일 상세:**
```
┌──────────────────────────────┐
│ [☰]  WIGVU           [🌐∨] │  ← 모바일 상단 바 (사이드바 대체)
├──────────────────────────────┤
│                              │
│  한국어, 읽으면서 배우세요     │
│  AI가 문장별 번역해드려요      │
│                              │
│  ┌──────────────────────┐   │
│  │ Beginner             │   │
│  │ 한국의 사계절과 문화   │   │
│  │ → 읽기 시작           │   │
│  └──────────────────────┘   │
│                              │
│  ┌──────────────────────┐   │
│  │ Intermediate         │   │
│  │ 한국어 존댓말의 이해   │   │
│  │ → 읽기 시작           │   │
│  └──────────────────────┘   │
│                              │
└──────────────────────────────┘
```

- 모바일에서는 **상단 미니 바** 표시: 햄버거 메뉴 + 로고 + 언어 드롭다운
- 햄버거 클릭 → 사이드바 오버레이 (좌측에서 슬라이드)
- 오버레이 배경: `rgba(0,0,0,0.3)` 딤 처리

### 2.8 Section 8.2 — 파일 구조 (전면 교체)

**삭제:**

| 파일 | 삭제 이유 |
|------|----------|
| `components/tab-navigation.tsx` | 탭 → 사이드바 전환 |
| `components/study-hero.tsx` | 큰 히어로 제거 |
| `components/video-hero.tsx` | `/video` 페이지로 분리 |
| `components/navigation.tsx` 리디자인 | 상단 헤더 제거 |

**신규/수정:**

| 파일 | 역할 | 우선순위 |
|------|------|---------|
| `components/layout/sidebar.tsx` | 사이드바 전체 (접기/펼치기, 항목, 유틸리티) | P0 |
| `components/layout/sidebar-item.tsx` | 개별 사이드바 항목 (아이콘+텍스트, 활성 상태) | P0 |
| `components/layout/mobile-header.tsx` | 모바일 상단 바 (햄버거+로고+언어) | P0 |
| `components/layout/sidebar-overlay.tsx` | 모바일 사이드바 오버레이 | P0 |
| `app/layout.tsx` | 사이드바 + 콘텐츠 영역 글로벌 레이아웃 | P0 |
| `app/page.tsx` | `/` — 헤드라인 + 글 목록 (간결) | P0 |
| `app/video/page.tsx` | `/video` — 수직 중앙 URL 인풋 + 기능 카드 | P0 |
| `components/language-selector.tsx` | 사이드바 내 드롭다운 (칩 그룹에서 변경) | P1 |
| `components/study/article-list.tsx` | 카드 디자인 업데이트 | P0 |
| `components/study/article-reader.tsx` | 읽기 페이지 디자인 업데이트 | P0 |
| `components/study/study-mode-panel.tsx` | 학습 패널 디자인 업데이트 | P0 |
| `components/study/expression-card.tsx` | 표현 카드 디자인 업데이트 | P0 |

### 2.9 Section 8.3 — 상태 관리 (수정)

**현재 PRD:**
```typescript
interface MainPageState {
  activeTab: "study" | "video";
  selectedLanguage: string;
}
```

**수정안:**
```typescript
// 탭 상태 제거 — URL 기반 라우팅으로 대체

interface SidebarState {
  isExpanded: boolean;          // 사이드바 펼침/접힘
  isMobileOpen: boolean;        // 모바일 오버레이 열림/닫힘
}

interface AppState {
  selectedLanguage: string;     // 학습 언어 (기본: "en")
  // localStorage에 persist: selectedLanguage, sidebarExpanded
}
```

- `activeTab` 제거: URL pathname으로 판별 (`/` = 읽기, `/video` = 영상)
- `isExpanded`: 데스크탑/태블릿에서 토글, localStorage에 저장
- `isMobileOpen`: 모바일 햄버거 오버레이 상태
- `selectedLanguage`: 사이드바 드롭다운에서 변경, localStorage에 저장

### 2.10 Section 9 — Implementation Phases (재구성)

**Phase 1: 디자인 시스템 + 사이드바 레이아웃 기반**
- [ ] `globals.css` 컬러 시스템 교체 (Light/Dark 토큰)
- [ ] Pretendard 폰트 설정 (`layout.tsx`)
- [ ] 타이포그래피 스케일 CSS 클래스
- [ ] `layout.tsx`에 사이드바 + 콘텐츠 영역 글로벌 레이아웃
- [ ] `sidebar.tsx` 구현 (펼침/접힘, 항목, 활성 상태)
- [ ] `sidebar-item.tsx` 구현
- [ ] `mobile-header.tsx` + `sidebar-overlay.tsx` 구현
- [ ] 기존 YouTube 다크모드 전용 스타일 제거
- [ ] `hero-section.tsx`, `klaim-pricing-table.tsx` 삭제

**Phase 2: 메인 페이지 + Video 페이지**
- [ ] `page.tsx` 재작성 — 간결한 헤드라인 + 글 목록
- [ ] `language-selector.tsx` (사이드바 드롭다운)
- [ ] `app/video/page.tsx` — 수직 중앙 URL 인풋 + 기능 카드
- [ ] 기존 `page.tsx` 모놀리식 코드 완전 제거

**Phase 3: Study 컴포넌트 디자인 업데이트**
- [ ] `article-list.tsx` 카드 디자인 (새 컬러/타이포)
- [ ] `article-reader.tsx` 읽기 페이지 (720px, 가독성)
- [ ] `study-mode-panel.tsx` 학습 패널
- [ ] `expression-card.tsx` 표현 카드

**Phase 4: Video 페이지 & 마무리**
- [ ] `analyze/[videoId]` 페이지 컬러 시스템 적용
- [ ] 기존 analysis 컴포넌트 컬러 토큰 교체
- [ ] 반응형 점검 (모바일/태블릿/데스크탑)
- [ ] 다크모드 전체 점검
- [ ] 불필요한 CSS/컴포넌트/import 정리
- [ ] `next build` 빌드 검증

---

## 3. 결정이 필요한 사항

| # | 질문 | 옵션 | 권장 |
|---|------|------|------|
| Q1 | 액센트 컬러 | A: 블루 `#2563EB` / B: 그린 `#16A34A` | A (블루) — 레벨 컬러 충돌 방지 |
| Q2 | 사이드바 "내 학습" 예약 슬롯 | A: 포함 (비활성) / B: 제외 | B (제외) — MVP에서 복잡도 최소화 |
| Q3 | 모바일 네비게이션 | A: 햄버거+오버레이 / B: 하단 네비 바 | A (햄버거) — Liner와 동일 패턴 |

---

## 4. 수정 요약 — 파일 영향도

### PRD에서 변경될 섹션

| 섹션 | 변경 수준 | 내용 |
|------|----------|------|
| 1.2 Goals — G3 | 문구 수정 | "탭 네비게이션" → "사이드바 네비게이션" |
| 3.1 Page Structure | 전면 재설계 | 라우팅 구조 변경 |
| 3.2 Navigation | 전면 교체 | 상단 탭 → 사이드바 |
| 4.1 Study 탭 | 전면 재설계 | 히어로 축소, 사이드바 연동 |
| 4.2 Video 탭 | 전면 재설계 | 별도 `/video` 페이지 |
| 6.1 네비게이션 FR | 전면 교체 | FR-001~005 삭제, 새 FR |
| 6.2 Study FR | 부분 수정 | 언어 칩→드롭다운, 히어로 축소 |
| 6.3 Video FR | 부분 수정 | 탭→페이지 |
| 7.3 Responsive | 전면 재설계 | 사이드바 브레이크포인트 |
| 8.2 파일 구조 | 전면 교체 | 탭 컴포넌트→사이드바 컴포넌트 |
| 8.3 상태 관리 | 전면 교체 | activeTab→사이드바 상태 |
| 9 Implementation | 전면 재구성 | Phase 1에 사이드바 추가 |

### 구현에서 변경될 파일 (최종)

**삭제 (4개):**
| 파일 | 이유 |
|------|------|
| `hero-section.tsx` | 캐러셀 제거 |
| `klaim-pricing-table.tsx` | 프라이싱 제거 |
| 기존 `page.tsx` 모놀리식 코드 | 완전 재작성 |
| `navigation.tsx` (상단 헤더) | 사이드바로 대체 |

**신규 (6개):**
| 파일 | 역할 |
|------|------|
| `components/layout/sidebar.tsx` | 사이드바 메인 |
| `components/layout/sidebar-item.tsx` | 사이드바 항목 |
| `components/layout/mobile-header.tsx` | 모바일 상단 바 |
| `components/layout/sidebar-overlay.tsx` | 모바일 사이드바 오버레이 |
| `components/language-selector.tsx` | 언어 드롭다운 |
| `app/video/page.tsx` | 영상 분석 전용 페이지 |

**수정 (8개):**
| 파일 | 변경 |
|------|------|
| `globals.css` | 컬러/타이포 전면 교체 + 사이드바 변수 추가 |
| `app/layout.tsx` | Pretendard + 사이드바 레이아웃 |
| `app/page.tsx` | 간결한 헤드라인 + 글 목록 |
| `components/study/article-list.tsx` | 카드 디자인 업데이트 |
| `components/study/article-reader.tsx` | 읽기 디자인 업데이트 |
| `components/study/study-mode-panel.tsx` | 학습 패널 업데이트 |
| `components/study/expression-card.tsx` | 표현 카드 업데이트 |
| `analyze/[videoId]` 관련 | 컬러 토큰 교체 |

---

## 다음 단계

이 수정안이 확정되면:
1. `PRD_ui-overhaul.md`를 v2로 업데이트
2. `PLAN_ui-overhaul.md`도 새 Phase 구조로 업데이트
3. `/implement` 실행
