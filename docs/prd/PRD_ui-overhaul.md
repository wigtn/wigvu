# WIGVU UI Overhaul PRD

> **Version**: 2.1
> **Created**: 2026-02-13
> **Updated**: 2026-02-13
> **Status**: Draft
> **Reference**: Liner (liner.com) — 사이드바 네비게이션, 콘텐츠 중앙 정렬

## 1. Overview

### 1.1 Problem Statement

현재 WIGVU는 YouTube 동영상 분석 도구로 시작했지만, 핵심 가치는 **한국어 학습 플랫폼**으로 이동했다. 그러나 UI는 여전히 동영상 분석 중심의 YouTube 다크모드 테마를 유지하고 있어, 학습 앱으로서의 정체성과 사용자 경험이 불일치한 상태다.

**현재 문제점:**
- 랜딩 페이지가 마케팅 캐러셀 중심 — 핵심 기능(학습)까지 스크롤 필요
- YouTube 다크모드 톤 — 학습 앱에 부적합한 무거운 느낌
- 동영상 분석과 한국어 학습이 한 페이지에 혼재
- 650줄짜리 모놀리식 `page.tsx` — 유지보수 어려움
- 온보딩 없이 바로 복잡한 UI 노출

### 1.2 Goals

| ID | Goal | 측정 기준 |
|----|------|----------|
| G1 | Study-First 구조로 전환 | 첫 화면에서 학습 글 목록 스크롤 없이 즉시 노출 |
| G2 | Clean & Minimal 디자인 시스템 적용 | 라이트 모드 기본, 화이트스페이스 중심 |
| G3 | 사이드바 네비게이션으로 기능 분리 | Liner 스타일 좌측 사이드바 (접기/펼치기) |
| G4 | 콘텐츠 중심 레이아웃 | 불필요한 마케팅 요소 제거, 앱 도구 느낌 |
| G5 | 모바일 최적화 | 햄버거 메뉴 + 사이드바 오버레이 |

### 1.3 Non-Goals (Out of Scope)

- 회원가입/로그인 시스템 (Phase 1 제외)
- 학습 진도 저장 (DB 필요, 추후)
- 학습 콘텐츠 DB 저장 (사용자 입력 텍스트는 세션 단위)
- 동영상 분석 기능 자체의 변경 (UI 배치만 변경)
- 다국어 UI (한국어 UI 유지, 학습 콘텐츠만 다국어)
- 사이드바 "내 학습" 예약 슬롯 (MVP 범위 밖)

### 1.4 Scope

| 포함 | 제외 |
|------|------|
| 전체 컬러 시스템 교체 | 백엔드 API 변경 |
| 사이드바 네비게이션 신규 구현 | 새로운 기능 추가 |
| 페이지 구조 재설계 (라우팅 변경) | 데이터베이스 도입 |
| 컴포넌트 리팩토링 | 인증/인가 시스템 |
| 타이포그래피 체계 | 결제 시스템 변경 |

---

## 2. Design Direction

### 2.1 Design References

| 서비스 | 참고 요소 |
|--------|----------|
| **라이너 (liner.com)** | 좌측 사이드바, 접기/펼치기, 섹션 레이블, 활성 항목 스타일, 콘텐츠 수직 중앙 정렬 |
| **Notion** | 화이트스페이스, 타이포그래피 위계, 미니멀 아이콘 |
| **Linear** | 깔끔한 사이드바, 컬러 시스템, 인터랙션 |
| **뤼튼 (wrtn.ai)** | Input 중심 히어로, 기능 카드 그리드 |

### 2.2 Design Principles

```
1. Content-First     — 콘텐츠가 주인공, UI는 조용히
2. Breathe           — 화이트스페이스를 아끼지 않는다
3. Typography-Driven — 색보다 글자 크기와 굵기로 위계를 만든다
4. Functional Beauty — 장식 없이 기능 자체가 아름답다
5. App-like          — 마케팅 페이지가 아닌 도구 진입점
```

### 2.3 Color System

**Light Mode (기본):**

| Token | Color | 용도 |
|-------|-------|------|
| `--background` | `#FFFFFF` | 메인 배경 |
| `--background-secondary` | `#F8F9FA` | 카드/섹션 배경, 사이드바 활성 항목 |
| `--foreground` | `#111111` | 본문 텍스트 |
| `--foreground-secondary` | `#6B7280` | 보조 텍스트, 사이드바 비활성 항목 |
| `--accent` | `#2563EB` | 주요 인터랙션, 사이드바 활성 보더 |
| `--accent-light` | `#EFF6FF` | 액센트 배경 |
| `--border` | `#E5E7EB` | 구분선, 사이드바 보더 |
| `--surface` | `#FFFFFF` | 카드 표면 |
| `--sidebar-bg` | `#FFFFFF` | 사이드바 배경 |
| `--sidebar-section` | `#9CA3AF` | 사이드바 섹션 레이블 |

**Dark Mode (선택):**

| Token | Color | 용도 |
|-------|-------|------|
| `--background` | `#0A0A0A` | 메인 배경 |
| `--background-secondary` | `#141414` | 카드/섹션 배경 |
| `--foreground` | `#FAFAFA` | 본문 텍스트 |
| `--foreground-secondary` | `#A1A1AA` | 보조 텍스트 |
| `--accent` | `#3B82F6` | 주요 인터랙션 |
| `--accent-light` | `#1E293B` | 액센트 배경 |
| `--border` | `#27272A` | 구분선 |
| `--surface` | `#141414` | 카드 표면 |
| `--sidebar-bg` | `#0A0A0A` | 사이드바 배경 |
| `--sidebar-section` | `#71717A` | 사이드바 섹션 레이블 |

**Semantic Colors:**

| Token | Light | Dark | 용도 |
|-------|-------|------|------|
| `--success` | `#16A34A` | `#22C55E` | 성공/완료 |
| `--warning` | `#F59E0B` | `#FBBF24` | 경고 |
| `--error` | `#DC2626` | `#EF4444` | 에러 |
| `--info` | `#2563EB` | `#3B82F6` | 정보 |

**Level Colors (학습 난이도):**

| Level | Color | Badge 배경 |
|-------|-------|-----------|
| Beginner | `#16A34A` | `#F0FDF4` |
| Intermediate | `#F59E0B` | `#FFFBEB` |
| Advanced | `#DC2626` | `#FEF2F2` |

### 2.4 Typography

```
Font Family: Pretendard (한/영 겸용, 가독성 우수)
Fallback: -apple-system, BlinkMacSystemFont, system-ui, sans-serif

Scale:
  Display    — 36px / 700 / -0.02em  (페이지 타이틀 — /video 중앙 헤드라인)
  Heading 1  — 24px / 700 / -0.01em  (콘텐츠 영역 헤드라인)
  Heading 2  — 22px / 600 / 0        (카드 타이틀)
  Heading 3  — 18px / 600 / 0        (소제목)
  Body       — 16px / 400 / 0        (본문)
  Body Small — 14px / 400 / 0        (보조 텍스트, 사이드바 항목)
  Caption    — 12px / 500 / 0.02em   (레이블, 배지, 사이드바 섹션)

Korean Text (학습 콘텐츠):
  Reading    — 18px / 400 / line-height 1.8  (읽기 모드)
  Study      — 16px / 400 / line-height 1.6  (학습 모드)
```

### 2.5 Spacing & Layout

```
Base Unit: 4px

Spacing Scale:
  xs   = 4px    (아이콘과 텍스트 사이)
  sm   = 8px    (인라인 요소 간)
  md   = 16px   (요소 간 기본 간격)
  lg   = 24px   (섹션 내 간격)
  xl   = 32px   (섹션 간 간격)
  2xl  = 48px   (큰 섹션 간)
  3xl  = 64px   (페이지 섹션 간)

Sidebar:
  expanded-width: 240px
  collapsed-width: 48px

Content Area:
  max-width: 960px (카드 그리드)
  reading-width: 720px (읽기 모드)
  padding: 16px (mobile), 24px (tablet), 32px (desktop)

Border Radius:
  sm   = 6px    (배지, 작은 요소)
  md   = 8px    (카드, 버튼, 사이드바 항목)
  lg   = 12px   (큰 카드, 인풋 카드, 모달)
  full = 9999px (pill 모양)
```

---

## 3. Information Architecture

### 3.1 Page Structure

```
/                          → 메인 (사이드바 "읽기" 기본 선택)
                             └─ Liner Scholar 스타일: 텍스트/URL 인풋 중심
                             └─ 하단에 추천 글 링크 (실제 한국어 뉴스/에세이)
/study                     → 학습 화면 (텍스트 제출 후 즉시 학습 모드)
                             └─ 원문+번역 교차 배치 + 표현 카드
                             └─ 보기 모드 토글 (교차/원문만/번역만)
                             └─ 하단: 표현 복습 요약
/video                     → 영상 분석 (사이드바 "영상 분석" 선택)
                             └─ 수직 중앙 URL 인풋 + 기능 소개
/analyze/[videoId]         → 동영상 분석 결과
```

### 3.2 Navigation — 좌측 사이드바

상단 헤더를 제거하고, Liner 스타일의 좌측 사이드바로 모든 네비게이션을 통합한다.

**펼친 상태 (Expanded — 240px):**
```
┌──────────────────────────┐
│ 🇰🇷 WIGVU           [⊞] │  ← 로고 + 접기 토글
│                          │
│ ── 학습 ──               │  ← 섹션 레이블
│ 📖 읽기                  │  ← (/) 학습 글 목록
│                          │
│ ── 도구 ──               │  ← 섹션 레이블
│ 🎬 영상 분석             │  ← (/video) YouTube URL 입력
│                          │
│                          │
│                          │  ← 여유 공간 (향후 확장)
│                          │
│ ── ── ── ──              │  ← 구분선
│ 🌐 English ∨            │  ← 학습 언어 드롭다운
│ ☀️ 라이트 모드            │  ← 테마 토글
│ GitHub ↗                │  ← 외부 링크
└──────────────────────────┘
```

**접힌 상태 (Collapsed — 48px):**
```
┌──────┐
│ 🇰🇷   │  ← 로고 아이콘
│      │
│ 📖   │  ← 읽기 (툴팁)
│      │
│ 🎬   │  ← 영상 분석 (툴팁)
│      │
│      │
│ 🌐   │  ← 언어
│ ☀️   │  ← 테마
└──────┘
```

**사이드바 항목:**

| 항목 | 아이콘 (Lucide) | 라우트 | 활성 조건 |
|------|----------------|--------|----------|
| 읽기 | `BookOpen` | `/` | `pathname === "/"` 또는 `/study` |
| 영상 분석 | `Film` | `/video` | `pathname === "/video"` 또는 `/analyze/*` |
| 언어 선택 | `Globe` | - | 드롭다운 (7개 언어) |
| 테마 토글 | `Sun` / `Moon` | - | 라이트/다크 전환 |
| GitHub | `Github` | external | 새 탭에서 열림 |

**활성 항목 스타일 (Liner 참고):**

| 상태 | 스타일 |
|------|--------|
| 비활성 | `color: --foreground-secondary`, `bg: transparent`, `border-left: 2px solid transparent` |
| 활성 | `color: --foreground`, `bg: --background-secondary`, `border-left: 2px solid --accent`, `font-weight: 500` |
| 호버 | `bg: --background-secondary` |

**사이드바 디자인 규칙:**

| 요소 | 스타일 |
|------|--------|
| 섹션 레이블 | 12px, `--sidebar-section`, 대문자/소문자, 좌측 정렬 |
| 항목 텍스트 | 14px, 좌측 아이콘(16px) + 12px 간격 + 텍스트 |
| 항목 패딩 | `8px 12px` (세로 8, 가로 12) |
| 접기 토글 | 로고 옆 `⊞` 아이콘 (Lucide `PanelLeftClose` / `PanelLeftOpen`) |
| 구분선 | `1px solid --border`, 좌우 12px 마진 |

**사이드바 반응형:**

| Breakpoint | 사이드바 상태 | 기본 |
|------------|-------------|------|
| Desktop (>1024px) | 펼침 (expanded, 240px) | 펼침 |
| Tablet (640-1024px) | 접힘 (collapsed, 48px) | 접힘 |
| Mobile (<640px) | 숨김 + 햄버거 오버레이 | 숨김 |

---

## 4. Page Designs

### 4.1 메인 페이지 — `/` (읽기 입력)

Liner Scholar 스타일: 인풋 중심, 수직 중앙 정렬. 사용자가 직접 한국어 텍스트를 가져오는 구조.

```
┌─────────┬──────────────────────────────────────────┐
│         │                                          │
│ Sidebar │              (상단 여백 ~15vh)            │
│         │                                          │
│ 📖 읽기  │     한국어, 읽으면서 배우세요              │  ← H1 (24px, 볼드, 중앙)
│  (활성)  │     텍스트를 붙여넣으면 AI가              │  ← Body Small (14px, gray, 중앙)
│         │     문장별로 번역해드려요                   │
│ 🎬 영상  │                                          │
│         │  ┌──────────────────────────────────┐    │
│         │  │                                  │    │
│         │  │  한국어 텍스트 또는 URL을          │    │  ← 텍스트에어리어
│         │  │  붙여넣으세요...                   │    │     border: 1px solid --border
│         │  │                                  │    │     border-radius: 12px
│         │  │                                  │    │     min-height: 120px
│         │  │                                  │    │
│ ── ── ──│  │                        [학습 →]  │    │  ← CTA 버튼
│ 🌐 EN∨ │  └──────────────────────────────────┘    │
│ ☀️      │                                          │
│ GitHub  │     추천 글                               │  ← 섹션 레이블 (Caption, gray)
│         │     · 겨울철 한파 대비 행동요령 — KBS      │  ← 실제 뉴스/에세이 링크
│         │     · 한국의 커피 문화 이야기 — 브런치     │
│         │     · 서울 지하철 이용 가이드 — Visit Seoul│
│         │     · 한국어 존댓말, 왜 중요할까 — 한겨레  │
│         │     · K-드라마로 배우는 일상 표현 — 에세이  │
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**인풋 영역:**
- 텍스트에어리어 (멀티라인): `max-width: 640px`, `min-height: 120px`
- Liner 검색창 스타일: `border: 1px solid --border`, `border-radius: 12px`, `padding: 16px 20px`
- 플레이스홀더: "한국어 텍스트 또는 URL을 붙여넣으세요..."
- 그림자 없음, 배경 `--surface`
- focus 시: `border-color: --accent`
- 하단 우측: [학습 시작 →] 버튼 (`--accent` 컬러)
- URL 감지: `http(s)://` 로 시작하면 URL 모드로 전환 (아이콘 변경)

**URL 입력 시 동작:**
- URL 패턴 감지 → 인풋 하단에 "URL에서 텍스트를 가져옵니다" 표시
- 제출 시 서버에서 URL 스크래핑 → 본문 텍스트 추출 → AI 분석
- 스크래핑 실패 시: "텍스트를 가져올 수 없습니다. 직접 붙여넣어주세요." 에러

**추천 글 링크:**
- 인풋 카드 아래 `margin-top: 32px`
- "추천 글" 섹션 레이블 (Caption 12px, `--foreground-secondary`)
- 5개 실제 한국어 콘텐츠 링크 (뉴스, 에세이, 블로그)
- 각 링크: 텍스트 + 출처 표시, 14px, `--accent` 컬러
- 클릭 시: URL이 인풋에 자동 입력 → 바로 학습 시작
- 링크는 하드코딩 (추후 API로 동적 추천 가능)

### 4.2 학습 화면 — `/study`

텍스트 제출 즉시 학습 모드 진입. 원문+번역 교차 배치가 기본.

```
┌─────────┬──────────────────────────────────────────┐
│         │                                          │
│ Sidebar │  [← 새 글] 겨울철 한파 대비 행동요령       │  ← 뒤로가기 + 제목 (H2, 22px)
│         │                                          │
│ 📖 읽기  │  [교차]  [원문만]  [번역만]               │  ← 보기 모드 토글 (pill 버튼)
│  (활성)  │  ─────────────────────────────────       │
│         │                                          │
│ 🎬 영상  │  겨울철에는 한파에 대비하여 건강 관리에     │  ← 한국어 원문 (18px, bold 약간)
│         │  각별히 신경 써야 합니다.                   │
│         │                                          │
│         │  In winter, you need to pay special      │  ← 번역 (16px, --foreground-secondary)
│         │  attention to health care to prepare     │     들여쓰기 + 좌측 2px 액센트 보더
│         │  for cold waves.                         │
│         │                                          │
│         │  특히 어르신과 어린이는 외출 시             │  ← 한국어 원문
│         │  방한 용품을 꼭 챙기시기 바랍니다.          │
│         │                                          │
│         │  Especially, elderly and children should │  ← 번역
│         │  make sure to bring warm clothing        │
│         │  when going out.                         │
│         │                                          │
│ ── ── ──│  ...                                     │
│ 🌐 EN∨ │                                          │
│ ☀️      │  ── 핵심 표현 (6개) ─────────────────     │  ← 표현 섹션 (스크롤 하단)
│ GitHub  │                                          │
│         │  ┌──────────────────────────────────┐    │
│         │  │ 한파에 대비하여                     │    │  ← 표현 카드
│         │  │ "to prepare for cold waves"       │    │
│         │  │ collocation · 문장 1               │    │
│         │  └──────────────────────────────────┘    │
│         │                                          │
│         │  ┌──────────────────────────────────┐    │
│         │  │ 각별히 신경 쓰다                    │    │
│         │  │ "to pay special attention"        │    │
│         │  │ collocation · 문장 1               │    │
│         │  └──────────────────────────────────┘    │
│         │                                          │
│         │  ── 표현 복습 요약 ───────────────────    │  ← 학습 완료 섹션
│         │                                          │
│         │  이 글에서 학습한 표현                     │
│         │                                          │
│         │  숙어 (2)                                │
│         │  · 한파에 대비하여 — to prepare for...   │
│         │  · 신경을 쓰다 — to pay attention to    │
│         │                                          │
│         │  문법 패턴 (1)                            │
│         │  · -(으)시기 바랍니다 — polite request   │
│         │                                          │
│         │  [새 글 학습하기 →]                       │  ← / 로 돌아가기
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**보기 모드 토글:**

| 모드 | 동작 |
|------|------|
| 교차 (기본) | 한국어 문장 → 번역 → 한국어 → 번역... 반복 |
| 원문만 | 한국어 문장만 표시, 번역 숨김 |
| 번역만 | 번역만 표시, 한국어 원문 숨김 |

- 토글 UI: pill 형태 3개 버튼 (Segmented Control)
- 전환 시 부드러운 애니메이션 (`opacity`, `max-height`)

**문장 배치 스타일:**
- 한국어 원문: 18px, `--foreground`, `font-weight: 500`, `line-height: 1.8`
- 번역: 16px, `--foreground-secondary`, 좌측 `2px solid --accent-light` 보더, `padding-left: 16px`
- 문장 간 간격: `24px`
- 콘텐츠 영역: `max-width: 720px`, 중앙 정렬

**표현 카드:**
- 문장 목록 아래에 "핵심 표현 (N개)" 섹션
- 카드: `bg: --background-secondary`, `border-radius: 8px`, `padding: 16px`
- 카드 내용: 표현 (한국어, 18px bold) + 의미 (번역, 14px) + 카테고리 배지 + 출처 문장 번호
- 카드 간격: `12px`

**표현 복습 요약 (학습 완료):**
- 스크롤 최하단에 위치
- 카테고리별 그룹핑: 숙어, 구어체, 문법 패턴, 콜로케이션, 격식 표현
- 각 카테고리: 제목 (카테고리명 + 개수) + 표현 리스트
- 표현 리스트: `· 한국어 표현 — 번역` 형식
- 하단: [새 글 학습하기 →] 버튼 → `/` 로 이동

**로딩 상태:**
- 텍스트 제출 → AI 분석 중: 스켈레톤 UI (문장 형태 placeholder)
- 프로그레스 표시: "분석 중..." 텍스트 + 스피너
- 평균 소요: 3-10초 (텍스트 길이에 따라)

**에러 처리:**
- AI 분석 실패: "분석에 실패했습니다. 다시 시도해주세요." + [다시 시도] 버튼
- 텍스트 너무 김: "15,000자를 초과했습니다. 텍스트를 줄여주세요."
- URL 스크래핑 실패: "해당 URL에서 텍스트를 가져올 수 없습니다."

### 4.3 영상 분석 페이지 — `/video`

Liner 홈페이지 스타일: 수직 중앙 정렬, 헤드라인 + 인풋 카드.

```
┌─────────┬──────────────────────────────────────────┐
│         │                                          │
│ Sidebar │              (상단 여백 ~20vh)            │
│         │                                          │
│ 📖 읽기  │     YouTube 영상, 핵심만 빠르게           │  ← H1 (24px, 볼드, 중앙)
│         │     URL을 붙여넣으면 AI가 요약하고         │  ← Body Small (14px, gray, 중앙)
│ 🎬 영상  │     핵심 구간을 찾아드려요                 │
│  (활성)  │                                          │
│         │  ┌──────────────────────────────────┐    │
│         │  │                                  │    │
│         │  │  YouTube URL을 입력하세요...      │    │  ← 인풋 카드
│         │  │                                  │    │     border: 1px solid --border
│         │  │                        [분석 →]  │    │     border-radius: 12px
│ ── ── ──│  └──────────────────────────────────┘    │     padding: 16px 20px
│ 🌐 EN∨ │                                          │
│ ☀️      │     ┌────────┐ ┌────────┐ ┌────────┐    │
│ GitHub  │     │ 📝     │ │ 🎯     │ │ 🌐     │    │  ← 기능 카드 3개
│         │     │ AI 요약 │ │ 핵심구간│ │ 자막번역│    │     3열 그리드
│         │     │ 영상의  │ │ 중요한  │ │ 외국어  │    │     텍스트 중앙정렬
│         │     │ 핵심을  │ │ 순간을  │ │ 영상도  │    │
│         │     │ 정리    │ │ 타임라인│ │ 한국어로│    │
│         │     └────────┘ └────────┘ └────────┘    │
│         │                                          │
└─────────┴──────────────────────────────────────────┘
```

**레이아웃:**
- 콘텐츠 수직 중앙 정렬 (`flex`, `items-center`, `justify-center`, `min-h-screen`)
- 헤드라인 + 서브텍스트: 중앙 정렬
- 인풋 카드: `max-width: 640px`, Liner 검색창 스타일
- 기능 카드 3개: 인풋 카드 아래 `margin-top: 48px`

**인풋 카드 스타일 (Liner 검색창 참고):**
- `border: 1px solid --border`
- `border-radius: 12px`
- `padding: 16px 20px`
- 그림자 없음, 배경 `--surface`
- focus 시: `border-color: --accent`
- 분석 버튼: 우측 정렬, `--accent` 컬러

### 4.4 동영상 분석 페이지 — `/analyze/[videoId]`

**기존 구조 유지, 컬러만 교체 + 사이드바 연동:**
- 사이드바: "영상 분석" 항목 활성 유지
- 다크모드 전용 → 시스템 테마 따라감
- 컬러 토큰만 새 시스템으로 교체
- 레이아웃/기능 변경 없음

### 4.5 모바일 레이아웃

모바일에서는 사이드바가 숨겨지고, 상단 미니 바로 대체.

**모바일 상단 바:**
```
┌──────────────────────────────┐
│ [☰]  WIGVU           [🌐∨] │  ← 48px 높이
└──────────────────────────────┘
```
- 좌측: 햄버거 메뉴 아이콘
- 중앙: WIGVU 로고
- 우측: 언어 선택 드롭다운

**햄버거 → 사이드바 오버레이:**
```
┌──────────────┬───────────────┐
│ 🇰🇷 WIGVU [✕] │               │
│              │               │
│ ── 학습 ──   │   (딤 배경)    │
│ 📖 읽기      │  rgba(0,0,0,  │
│              │    0.3)       │
│ ── 도구 ──   │               │
│ 🎬 영상 분석  │               │
│              │               │
│ ── ── ── ── │               │
│ ☀️ 라이트     │               │
│ GitHub ↗    │               │
└──────────────┴───────────────┘
```
- 좌측에서 슬라이드 인 (240px)
- 배경 딤 처리: `rgba(0,0,0,0.3)`
- 딤 영역 클릭 → 닫기
- 항목 클릭 → 페이지 이동 + 자동 닫기

**모바일 `/` 페이지:**
```
┌──────────────────────────────┐
│ [☰]  WIGVU           [🌐∨] │
├──────────────────────────────┤
│                              │
│  한국어, 읽으면서 배우세요     │
│  텍스트를 붙여넣으면           │
│  AI가 문장별로 번역해드려요    │
│                              │
│  ┌──────────────────────┐   │
│  │ 한국어 텍스트 또는    │   │
│  │ URL을 붙여넣으세요... │   │
│  │                      │   │
│  │             [학습 →] │   │
│  └──────────────────────┘   │
│                              │
│  추천 글                      │
│  · 겨울철 한파 대비 — KBS     │
│  · 한국의 커피 문화 — 브런치  │
│                              │
└──────────────────────────────┘
```

**모바일 `/study` 페이지:**
```
┌──────────────────────────────┐
│ [← 새 글]  WIGVU     [🌐∨] │
├──────────────────────────────┤
│ [교차] [원문만] [번역만]      │
│──────────────────────────────│
│                              │
│ 겨울철에는 한파에 대비하여    │
│ 건강 관리에 신경 써야 합니다. │
│                              │
│ │ In winter, you need to    │
│ │ pay special attention...  │
│                              │
│ ...                          │
│                              │
│ ── 핵심 표현 (6개) ──        │
│ ┌──────────────────────┐    │
│ │ 한파에 대비하여        │    │
│ │ "to prepare for..."   │    │
│ └──────────────────────┘    │
│                              │
└──────────────────────────────┘
```

---

## 5. User Stories

### US-01: 텍스트 붙여넣기로 학습 시작

```gherkin
Scenario: 한국어를 배우려는 외국인이 텍스트를 붙여넣어 학습한다
  Given 사용자가 WIGVU에 접속한다
  When 메인 페이지가 로드된다
  Then 사이드바에서 "읽기"가 활성화되어 있다
  And 중앙에 텍스트 입력 영역과 헤드라인이 보인다
  When 한국어 텍스트를 붙여넣고 [학습 시작] 버튼을 클릭한다
  Then /study 페이지로 이동한다
  And 원문+번역 교차 배치로 즉시 학습 모드가 시작된다
```

### US-02: URL로 학습 시작

```gherkin
Scenario: 학습자가 뉴스 기사 URL을 입력하여 학습한다
  Given 메인 페이지의 텍스트 입력 영역이 보인다
  When 한국어 뉴스 기사 URL을 붙여넣는다
  Then 인풋 하단에 "URL에서 텍스트를 가져옵니다" 안내가 표시된다
  When [학습 시작] 버튼을 클릭한다
  Then 서버가 URL에서 본문을 스크래핑한다
  And /study 페이지에서 추출된 텍스트로 학습이 시작된다
```

### US-03: 추천 글 링크로 학습 시작

```gherkin
Scenario: 학습자가 추천 글 링크를 클릭하여 학습한다
  Given 메인 페이지 인풋 아래에 추천 글 링크 목록이 보인다
  When "겨울철 한파 대비 행동요령 — KBS" 링크를 클릭한다
  Then 해당 URL이 인풋에 자동 입력된다
  And 바로 학습이 시작된다 (/study 페이지로 이동)
```

### US-04: 학습 중 보기 모드 전환

```gherkin
Scenario: 학습자가 원문만 보기 모드로 전환한다
  Given /study 페이지에서 교차 모드(한국어+번역)로 학습 중이다
  When 상단의 [원문만] 토글 버튼을 클릭한다
  Then 번역이 숨겨지고 한국어 원문만 표시된다
  When [교차] 버튼을 다시 클릭한다
  Then 원문+번역 교차 배치로 돌아온다
```

### US-05: 표현 복습 및 새 글 학습

```gherkin
Scenario: 학습자가 표현 복습을 확인하고 새 글을 학습한다
  Given /study 페이지에서 학습이 완료되어 스크롤 하단까지 왔다
  When 표현 복습 요약 섹션이 보인다
  Then 카테고리별(숙어, 문법 패턴, 구어체 등) 표현이 정리되어 있다
  When [새 글 학습하기 →] 버튼을 클릭한다
  Then / 페이지로 돌아와 새 텍스트를 입력할 수 있다
```

### US-06: 동영상 분석 전환

```gherkin
Scenario: 사용자가 동영상 분석 기능을 사용한다
  Given 사이드바 "읽기"가 활성화되어 있다
  When 사용자가 사이드바에서 "영상 분석"을 클릭한다
  Then /video 페이지로 이동한다
  And 사이드바 "영상 분석"이 활성화된다
  And 수직 중앙에 URL 입력 카드와 기능 소개가 보인다
```

### US-07: 모바일 사용

```gherkin
Scenario: 모바일에서 WIGVU를 사용한다
  Given 모바일 기기에서 접속한다
  When 메인 페이지가 로드된다
  Then 상단에 미니 바(햄버거+로고+언어)가 보인다
  And 텍스트 입력 영역이 화면 폭에 맞게 표시된다
  When 텍스트를 붙여넣고 학습을 시작한다
  Then /study 페이지에서 모바일에 최적화된 학습 화면이 보인다
```

### US-08: 사이드바 접기/펼치기

```gherkin
Scenario: 데스크탑에서 사이드바를 접는다
  Given 사이드바가 펼쳐진 상태다
  When 접기 토글 버튼을 클릭한다
  Then 사이드바가 48px 아이콘 모드로 전환된다
  And 콘텐츠 영역이 넓어진다
  And 아이콘에 마우스 호버 시 툴팁이 표시된다
```

---

## 6. Functional Requirements

### 6.1 사이드바 & 레이아웃

| ID | Requirement | Priority |
|----|------------|----------|
| FR-001 | 좌측 사이드바에 로고, 네비게이션, 유틸리티를 통합 | P0 |
| FR-002 | 사이드바 "읽기" 클릭 시 `/` 이동, "영상 분석" 클릭 시 `/video` 이동 | P0 |
| FR-003 | `/` 접속 시 사이드바 "읽기" 활성, 글 목록 즉시 표시 | P0 |
| FR-004 | 활성 항목: 좌측 2px `--accent` 보더 + `--background-secondary` 배경 | P0 |
| FR-005 | 접기/펼치기 토글 (접힌=48px 아이콘, 펼친=240px 아이콘+텍스트) | P1 |
| FR-006 | 데스크탑 기본 펼침, 태블릿 기본 접힘, 모바일 기본 숨김 | P1 |
| FR-007 | 모바일: 상단 미니 바(햄버거+로고+언어) + 사이드바 오버레이 | P0 |
| FR-008 | 상단 헤더 제거, 모든 네비게이션은 사이드바 (모바일은 미니 바) | P0 |
| FR-009 | 사이드바 펼침/접힘 상태를 localStorage에 저장 | P2 |

### 6.2 메인 페이지 — 입력 (`/`)

| ID | Requirement | Priority |
|----|------------|----------|
| FR-010 | 헤드라인(H1, 24px) + 서브텍스트(14px) 수직 중앙 정렬 (상단 여백 ~15vh) | P0 |
| FR-011 | 학습 언어 선택: 사이드바 하단 드롭다운 (en, ja, zh, es, vi, th, id), localStorage 저장 | P0 |
| FR-012 | 텍스트에어리어: `max-width: 640px`, `min-height: 120px`, `border-radius: 12px` | P0 |
| FR-013 | 텍스트 또는 URL 입력 → [학습 시작 →] 버튼 클릭 → `/study` 이동 | P0 |
| FR-014 | URL 패턴(`http(s)://`) 감지 시 "URL에서 텍스트를 가져옵니다" 안내 표시 | P1 |
| FR-015 | URL 제출 시 서버 스크래핑 → 본문 추출 → AI 분석 (실패 시 에러 메시지) | P1 |
| FR-016 | 인풋 아래 추천 글 링크 5개 (실제 한국어 뉴스/에세이 URL), 클릭 시 자동 학습 시작 | P1 |

### 6.3 영상 분석 페이지 (`/video`)

| ID | Requirement | Priority |
|----|------------|----------|
| FR-020 | 헤드라인 + URL 인풋 카드: 수직 중앙 정렬 (Liner 홈 스타일) | P0 |
| FR-021 | 인풋 카드: `border-radius: 12px`, `border: 1px solid --border`, 넉넉한 패딩 | P0 |
| FR-022 | URL 입력 후 Enter 또는 분석 버튼 → `/analyze/[videoId]` 이동 | P0 |
| FR-023 | 인풋 아래 기능 소개 카드 3개 (아이콘 + 제목 + 설명) | P1 |
| FR-024 | 기존 캐러셀, 프라이싱 섹션 삭제 (`hero-section.tsx`, `klaim-pricing-table.tsx`) | P0 |

### 6.4 디자인 시스템

| ID | Requirement | Priority |
|----|------------|----------|
| FR-030 | CSS 변수 기반 컬러 시스템 (Light/Dark + Sidebar 토큰) | P0 |
| FR-031 | Pretendard 폰트 적용 | P0 |
| FR-032 | 타이포그래피 스케일 적용 (Display ~ Caption) | P0 |
| FR-033 | 스페이싱 스케일 통일 (4px base) | P1 |
| FR-034 | 라이트 모드 기본, 다크 모드 토글 (사이드바에서) | P0 |

### 6.5 학습 화면 (`/study`)

| ID | Requirement | Priority |
|----|------------|----------|
| FR-040 | 콘텐츠 영역 `max-width: 720px` 중앙 정렬, 사이드바 "읽기" 활성 유지 | P0 |
| FR-041 | 상단: [← 새 글] 뒤로가기 + 제목(H2), 하단: 보기 모드 토글 (교차/원문만/번역만) | P0 |
| FR-042 | 기본 교차 모드: 한국어(18px, bold) → 번역(16px, gray, 좌측 액센트 보더) 반복 | P0 |
| FR-043 | [원문만] 클릭 시 번역 숨김, [번역만] 클릭 시 원문 숨김 (애니메이션 전환) | P1 |
| FR-044 | 문장 목록 아래 "핵심 표현 (N개)" 섹션: 표현 카드 리스트 | P0 |
| FR-045 | 스크롤 최하단: 표현 복습 요약 (카테고리별 그룹핑) + [새 글 학습하기 →] 버튼 | P1 |
| FR-046 | 로딩: 스켈레톤 UI + "분석 중..." 스피너. 에러: 재시도 버튼 | P0 |
| FR-047 | [← 새 글] 클릭 또는 사이드바 "읽기" 클릭 → `/` 복귀 | P0 |

---

## 7. Non-Functional Requirements

### 7.1 Performance

| Metric | Target |
|--------|--------|
| LCP (Largest Contentful Paint) | < 2.5s |
| CLS (Cumulative Layout Shift) | < 0.1 |
| FID (First Input Delay) | < 100ms |
| 번들 사이즈 증가 | < 50KB (Pretendard subset) |

### 7.2 Accessibility

| Requirement |
|-------------|
| 색상 대비 WCAG AA 기준 충족 (4.5:1 이상) |
| 사이드바 키보드 접근 가능 (Tab, Arrow, Enter) |
| 접힌 사이드바 아이콘에 툴팁 + aria-label |
| 스크린 리더 호환 (사이드바 `nav`, `aria-current="page"`) |
| 모션 감소 설정 존중 (`prefers-reduced-motion`) |
| 모바일 오버레이 ESC 키로 닫기 |

### 7.3 Responsive

| Breakpoint | Width | 사이드바 | 콘텐츠 영역 | 인풋/학습 |
|------------|-------|---------|-----------|-----------|
| Mobile | <640px | 숨김 + 햄버거 오버레이 | 100vw, 패딩 16px | 전폭 인풋 |
| Tablet | 640-1024px | 접힘 (48px) | `calc(100vw - 48px)`, 패딩 24px | max-width: 640px |
| Desktop | >1024px | 펼침 (240px) | `calc(100vw - 240px)`, 패딩 32px | max-width: 640px (인풋) / 720px (학습) |

---

## 8. Technical Design

### 8.1 삭제 대상

| 파일/컴포넌트 | 이유 |
|-------------|------|
| `hero-section.tsx` | 캐러셀 + SVG 애니메이션 제거 |
| `klaim-pricing-table.tsx` | 프라이싱 섹션 제거 |
| `navigation.tsx` (상단 헤더) | 사이드바로 대체 |
| 기존 `page.tsx` 모놀리식 코드 | 완전 재작성 |
| 캐러셀 관련 CSS/JS | 불필요 |

### 8.2 신규/수정 파일

| 파일 | 역할 | 유형 |
|------|------|------|
| `globals.css` | 컬러/타이포/스페이싱 전면 교체 + 사이드바 토큰 | 수정 |
| `app/layout.tsx` | Pretendard 폰트 + 사이드바 레이아웃 | 수정 |
| `components/layout/sidebar.tsx` | 사이드바 전체 (접기/펼치기, 항목, 유틸리티) | 신규 |
| `components/layout/sidebar-item.tsx` | 개별 사이드바 항목 (아이콘+텍스트, 활성 상태) | 신규 |
| `components/layout/mobile-header.tsx` | 모바일 상단 바 (햄버거+로고+언어) | 신규 |
| `components/layout/sidebar-overlay.tsx` | 모바일 사이드바 오버레이 | 신규 |
| `components/language-selector.tsx` | 사이드바 내 언어 드롭다운 | 신규 |
| `app/page.tsx` | Liner Scholar 스타일 텍스트/URL 인풋 + 추천 링크 | 수정 (재작성) |
| `app/study/page.tsx` | 학습 화면: 교차 배치 + 보기 모드 토글 + 표현 복습 | 신규 |
| `app/video/page.tsx` | 수직 중앙 URL 인풋 + 기능 카드 | 신규 |
| `components/study/study-input.tsx` | 메인 페이지 텍스트에어리어 + URL 감지 + CTA | 신규 |
| `components/study/sentence-view.tsx` | 교차/원문만/번역만 문장 렌더링 | 신규 |
| `components/study/view-mode-toggle.tsx` | 보기 모드 pill 토글 (교차/원문만/번역만) | 신규 |
| `components/study/expression-card.tsx` | 표현 카드 (새 디자인 시스템) | 수정 |
| `components/study/expression-summary.tsx` | 표현 복습 요약 (카테고리별 그룹핑) | 신규 |
| `components/study/recommended-links.tsx` | 추천 글 링크 목록 (실제 뉴스/에세이) | 신규 |

### 8.3 삭제 대상 (기존 Study 관련)

| 파일 | 이유 |
|------|------|
| `components/study/article-list.tsx` | 카드 그리드 제거 — 인풋 중심으로 전환 |
| `components/study/article-reader.tsx` | `/read/[articleId]` 라우트 제거 |
| `components/study/study-mode-panel.tsx` | 별도 학습 모드 진입 없음 — 즉시 학습 |
| `app/read/` 디렉토리 | `/study` 단일 라우트로 대체 |
| `data/articles.ts` (또는 유사) | 하드코딩 샘플 글 데이터 제거 |

### 8.4 상태 관리

```typescript
// 사이드바 상태
interface SidebarState {
  isExpanded: boolean;        // 접기/펼치기 (localStorage persist)
  isMobileOpen: boolean;      // 모바일 오버레이 열림/닫힘
}

// 앱 글로벌 상태
interface AppState {
  selectedLanguage: string;   // 학습 언어 (기본: "en", localStorage persist)
}

// 학습 화면 상태
type ViewMode = "interleaved" | "original" | "translation";

interface StudyPageState {
  viewMode: ViewMode;         // 보기 모드 (기본: "interleaved")
  inputText: string;          // 인풋 텍스트 (URL 또는 본문)
  isUrlMode: boolean;         // URL 감지 여부
}
```

- 활성 페이지: URL pathname으로 판별 (`/` 또는 `/study` = 읽기, `/video` = 영상)
- `isExpanded`: 데스크탑/태블릿에서 토글, localStorage에 저장
- `isMobileOpen`: 모바일 햄버거 오버레이, 페이지 이동 시 자동 닫힘
- `selectedLanguage`: 사이드바 드롭다운, localStorage에 저장
- `viewMode`: 학습 화면 내 토글, 세션 동안만 유지 (persist 안함)
- `/study` 페이지 데이터: 인풋 텍스트를 쿼리 파라미터 또는 sessionStorage로 전달

---

## 9. Implementation Phases

### Phase 1: 디자인 시스템 + 사이드바 레이아웃 기반

- [ ] `globals.css` 컬러 시스템 교체 (Light/Dark + Sidebar 토큰)
- [ ] Pretendard 폰트 설정 (`layout.tsx` — next/font 또는 CDN)
- [ ] 타이포그래피 스케일 CSS 유틸리티 클래스
- [ ] `layout.tsx`에 사이드바 + 콘텐츠 영역 글로벌 레이아웃
- [ ] `components/layout/sidebar.tsx` (펼침/접힘, 섹션, 유틸리티)
- [ ] `components/layout/sidebar-item.tsx` (아이콘, 텍스트, 활성 상태)
- [ ] `components/layout/mobile-header.tsx` (햄버거+로고+언어)
- [ ] `components/layout/sidebar-overlay.tsx` (모바일 오버레이)
- [ ] 기존 YouTube 다크모드 전용 스타일 제거
- [ ] `hero-section.tsx`, `klaim-pricing-table.tsx` 삭제

**Deliverable**: 사이드바가 동작하는 새 레이아웃 (콘텐츠는 임시)

### Phase 2: 메인 페이지 (인풋) + Video 페이지

- [ ] `app/page.tsx` 재작성 — Liner Scholar 스타일 텍스트/URL 인풋
- [ ] `components/study/study-input.tsx` (텍스트에어리어, URL 감지, CTA)
- [ ] `components/study/recommended-links.tsx` (추천 글 링크 5개)
- [ ] `components/language-selector.tsx` (사이드바 드롭다운)
- [ ] `app/video/page.tsx` — 수직 중앙 URL 인풋 + 기능 카드
- [ ] 기존 `page.tsx` 모놀리식 코드 완전 제거
- [ ] `navigation.tsx` 삭제

**Deliverable**: 사이드바 네비게이션으로 인풋/영상 페이지 전환 가능

### Phase 3: Study 학습 화면

- [ ] `app/study/page.tsx` — 학습 화면 (교차 배치 + 표현 카드 + 복습 요약)
- [ ] `components/study/sentence-view.tsx` (교차/원문만/번역만 문장 렌더링)
- [ ] `components/study/view-mode-toggle.tsx` (pill 토글 UI)
- [ ] `components/study/expression-card.tsx` 리디자인 (새 컬러 토큰)
- [ ] `components/study/expression-summary.tsx` (카테고리별 복습 요약)
- [ ] 로딩 스켈레톤 UI + 에러 처리 UI
- [ ] 기존 `article-list.tsx`, `article-reader.tsx`, `study-mode-panel.tsx` 삭제
- [ ] 기존 `app/read/` 디렉토리 삭제

**Deliverable**: 텍스트 입력 → 즉시 학습 모드 전체 플로우 완성

### Phase 4: Video 분석 + 마무리

- [ ] `analyze/[videoId]` 페이지 컬러 시스템 적용
- [ ] 기존 analysis 컴포넌트 컬러 토큰 교체
- [ ] 반응형 점검 (모바일/태블릿/데스크탑)
- [ ] 다크모드 전체 점검
- [ ] 불필요한 CSS/컴포넌트/import 정리
- [ ] `next build` 빌드 검증

**Deliverable**: 전체 UI 통일, 빌드 검증 완료

---

## 10. Success Metrics

| Metric | Before | Target | Measurement |
|--------|--------|--------|-------------|
| 첫 화면 → 학습 시작 | 스크롤 3회 + 글 선택 + Study Mode 클릭 | 텍스트 붙여넣기 → 바로 학습 (1 액션) | UX 플로우 |
| page.tsx 코드 라인 | ~650줄 | <100줄 (인풋 컴포넌트 분리) | `wc -l` |
| 컴포넌트 수 (레이아웃) | 1 모놀리식 | 10+ 분리 (sidebar, input, sentence-view...) | 파일 수 |
| Lighthouse Performance | 미측정 | >90 | Lighthouse |
| 번들 사이즈 | 현재 | 현재 이하 | `next build` |
| 사이드바 접기/펼치기 | 없음 | 3가지 상태 (펼침/접힘/숨김) | 기능 테스트 |
| 학습 보기 모드 | 없음 | 3가지 (교차/원문만/번역만) | 기능 테스트 |
